# reviews-plugin
Pulls Google Business Profile reviews directly into WordPress as a Custom Post Type (CPT), with each review stored as a post with ACF fields. This means the display is built entirely in a page builder with full design control—no third-party widget styling constraints, no SaaS subscription for display-only use cases.
